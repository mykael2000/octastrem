on:
  push:
    branches:
      - main
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: 📂 Sync files
        run: |
          sshpass -p "${{ secrets.FTP_PASSWORD }}" rsync -avz --delete ./ ${{ secrets.FTP_USERNAME }}@${{ secrets.FTP_SERVER }}:/octastrem.com/
        env:
          SSHPASS: ${{ secrets.FTP_PASSWORD }}
